---
import KeystaticAdmin from '../../components/KeystaticAdmin';

// 1. 强制预渲染，生成静态 index.html
export const prerender = true;
---

<!-- 2. 使用 client:only="react"，完全在客户端运行 -->
<KeystaticAdmin client:only="react" />