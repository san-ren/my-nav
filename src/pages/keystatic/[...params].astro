---
// src/pages/keystatic/[...params].astro
// ✅ 引入上面的组件 (默认导入，不带花括号)
import KeystaticPage from './page';

export const prerender = true;

export function getStaticPaths() {
  return [
    { params: { params: undefined } },
    { params: { params: 'cloud/oauth/callback' } }, // 👈 必须有这个，否则登录回调 404
    { params: { params: 'branch' } },
    { params: { params: 'collection' } },
    { params: { params: 'keystatic' } }
  ];
}
---

<KeystaticPage client:only="react" />