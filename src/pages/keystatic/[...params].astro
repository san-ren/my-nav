---
// src/pages/keystatic/[...params].astro

// 1. 引入我们刚刚创建的 React 组件 (注意路径是 ./page)
import { Keystatic } from './page';

// 2. 强制静态预渲染 (GitHub Pages 必须)
export const prerender = true;

// 3. 静态路径生成
export function getStaticPaths() {
  return [
    { params: { params: undefined } },
  ];
}
---

<Keystatic client:only="react" />