---
// 1. 确保从 UI 包导入 makePage
import { makePage } from '@keystatic/astro/ui';

// 2. 引入你的配置文件
// 注意：keystatic.config 此时已经是 .tsx 后缀，但引用时不需要写后缀
import config from '../../../keystatic.config';

export const prerender = false;
---

<!-- 
  3. 这里的调用方式非常关键 
  必须直接使用导入的 makePage.Keystatic，不要试图在 frontmatter 里把它赋值给变量再用
  client:only="react" 显式指定框架，有时能解决编译歧义
-->
<makePage.Keystatic config={config} client:only="react" />

<style is:global>
  /* 修复菜单遮挡问题 (保留) */
  div[data-tippy-root] {
    z-index: 2147483647 !important;
  }

  /* =======================================================
     🔥 CSS 魔法：保留功能，但隐藏 UI 图标
     通过 [title="..."] 选择器精准隐藏顶部工具栏按钮
     ======================================================= */

  /* 隐藏【加粗】按钮 (功能仍可用：快捷键 Ctrl+B 或 **text**) */
  div[role="toolbar"] button[title="Bold"] {
    display: none !important;
  }

  /* 隐藏【斜体】按钮 (功能仍可用：快捷键 Ctrl+I 或 *text*) */
  div[role="toolbar"] button[title="Italic"] {
    display: none !important;
  }

  /* 隐藏【表格】按钮 (功能仍可用) */
  /* 注意：表格通常没有 Markdown 快捷键，隐藏后只能通过斜杠菜单唤出 */
  div[role="toolbar"] button[title="Table"] {
    display: none !important;
  }
  
  /* 隐藏【代码块】按钮 */
  div[role="toolbar"] button[title="Code block"] {
    display: none !important;
  }

  /* 隐藏【引用】按钮 */
  div[role="toolbar"] button[title="Blockquote"] {
    display: none !important;
  }

  /* 隐藏【链接】按钮 */
  div[role="toolbar"] button[title="Link"] {
    display: none !important;
  }
  
  /* 隐藏【分割线】按钮 */
  /* Keystatic 的分割线按钮可能没有 title，需要检查 DOM */
  
  /* =======================================================
     🔥 高级：隐藏斜杠菜单 (Slash Menu) 中的选项
     警告：斜杠菜单是动态生成的列表，没有固定的 class。
     我们只能通过 nth-child (第几个) 来隐藏，这比较脆弱。
     如果官方更新了菜单顺序，CSS 可能会隐藏错东西。
     ======================================================= */
  
  /* 
     如果你非常想隐藏斜杠菜单里的原生选项，只留自定义组件，
     可以尝试隐藏前 N 个子元素。
     但我不推荐这样做，因为一旦你用鼠标滚轮，DOM 结构可能会变。
  */
  
  /* 示例：尝试隐藏斜杠菜单里的前 5 个选项 (通常是 Text, Heading 1-3 等) */
  /* 
  div[data-tippy-root] [role="menu"] > div > div:nth-child(-n+5) {
     display: none !important;
  } 
  */

</style>