---
// src/pages/keystatic/[...params].astro

// 1. 修改引入路径，指向 components 目录
import KeystaticAdmin from '../../components/KeystaticAdmin';

export const prerender = true;

export function getStaticPaths() {
  return [
    { params: { params: undefined } },
    { params: { params: 'cloud' } }, // 增加 cloud 路由
    { params: { params: 'cloud/oauth/callback' } },
    { params: { params: 'branch' } },
    { params: { params: 'collection' } },
    { params: { params: 'keystatic' } }
  ];
}
---

<KeystaticAdmin client:only="react" />