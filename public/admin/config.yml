backend:
  name: github
  repo: san-ren/my-nav
  branch: main
  auth_type: token
  
  commit_messages:
    create: '创建 {{collection}} “{{slug}}”'
    update: '更新 {{collection}} “{{slug}}”'
    delete: '删除 {{collection}} “{{slug}}”'
    uploadMedia: '上传资源 “{{path}}”'
    deleteMedia: '删除资源 “{{path}}”'

locale: 'zh_Hans'
media_folder: "public/images"
public_folder: "/images"

slug:
  encoding: "ascii"
  clean_accents: true

collections:
  # 1. 网站全局设置 (保持不变)
  - name: "site_settings"
    label: "⚙️ 网站基础配置"
    delete: false
    editor:
      preview: false
    files:
      - name: "config"
        label: "站点信息"
        file: "src/data/config.json"
        fields:
          - { label: "网站标题", name: "title", widget: "string", required: false }
          - { label: "GitHub 用户名", name: "githubUser", widget: "string", required: false }
          - { label: "GitHub 仓库名", name: "githubRepo", widget: "string", required: false }
          - { label: "页脚作者名", name: "author", widget: "string", required: false }

  # ============================================================
  # 2. 页面元数据管理 (只修改页面本身的属性)
  # ============================================================
  - name: "nav_pages_meta"
    label: "📄 页面管理 (添加/排序页面)"
    # 这里使用通配符匹配所有页面的 meta.json
    folder: "src/data/nav"
    path: "{{slug}}/meta" 
    extension: "json"
    format: "json"
    create: true
    identifier_field: "name"
    summary: "页面：{{name}} (ID: {{id}})"
    sortable_fields: ["sortOrder", "name"]
    editor:
      preview: false
    fields:
      - { label: "页面ID (文件夹名)", name: "id", widget: "string", required: true, pattern: ['^[a-zA-Z0-9-_]+$', "只能包含字母、数字、下划线"] }
      - { label: "侧边栏显示名称", name: "name", widget: "string", required: true }
      - { label: "侧边栏图标", name: "icon", widget: "string", required: false, hint: "Lucide图标名" }
      - { label: "排序权重", name: "sortOrder", widget: "number", default: 10, value_type: "int" }

  # ============================================================
  # 3. 分组内容管理 (核心内容，不再卡顿！)
  # ============================================================
  - name: "nav_groups"
    label: "📦 内容分组管理"
    # 使用通配符 ** 匹配任意页面下的 groups 文件夹
    folder: "src/data/nav"
    path: "{{page}}/groups/{{slug}}"
    media_folder: "" # 使用相对路径存储图片
    public_folder: ""
    extension: "json"
    format: "json"
    create: true
    identifier_field: "name"
    summary: "所属页面: {{page}} | 分组: {{name}}"
    editor:
      preview: false
    fields:
      # 这个隐藏字段用于在文件名中体现所属页面，虽然 CMS UI 不一定显示完美，但能工作
      - { label: "所属页面ID", name: "page", widget: "hidden", default: "home" } 
      
      - { label: "分组名称 (锚点标题)", name: "name", widget: "string", required: true }
      - { label: "分组图标", name: "icon", widget: "string", required: false }
      
      # --- 下面直接是 categories，不用再套 groups 了 ---
      - label: "内部分类"
        name: "categories"
        widget: "list"
        collapsed: false
        summary: "📑 分类：{{name}}"
        fields:
          - { label: "分类名称", name: "name", widget: "string", required: true }
          - { label: "分类图标", name: "icon", widget: "string", required: false }

          # --- 模式 A：无Tabs ---
          - label: "🔗 资源列表"
            name: "list"
            widget: "list"
            collapsed: true
            required: false
            summary: "{{name}}"
            fields: &site_fields
              - { label: "名称", name: "name", widget: "string", required: true }
              - { label: "链接地址", name: "url", widget: "string", required: false }
              # ... (这里省略你的原有字段，如 desc, detail, icon, badge_list, guide_id 等，保持不变) ...
              - { label: "简短描述", name: "desc", widget: "string", required: false }
              - { label: "详细说明", name: "detail", widget: "markdown", required: false }
              - { label: "自定义图标", name: "icon", widget: "image", required: false }
              - { label: "小工具链接", name: "widget_url", widget: "string", required: false }
              - { label: "教程ID", name: "guide_id", widget: "string", required: false }
              - label: "徽章"
                name: "badge_list"
                widget: "select"
                multiple: true
                options: ["stars", "version", "license", "last_commit"]

          # --- 模式 B：有Tabs ---
          - label: "🗃️ 标签页模式"
            name: "tabs"
            widget: "list"
            collapsed: true
            required: false
            fields:
              - { label: "页签名称", name: "tabName", widget: "string", required: true }
              - label: "资源列表"
                name: "list"
                widget: "list"
                collapsed: true
                fields: *site_fields


                
  # 🚀 新增集合：管理教程文章
  - name: "guides"
    label: "📚 教程文章管理"
    folder: "src/content/guides"
    extension: "md"
    create: true
    slug: "{{slug}}"
    identifier_field: "title"
    summary: "{{title}} - {{date | date('YYYY-MM-DD')}}"
    fields:
      - { label: "文章标题", name: "title", widget: "string", required: true }
      - { label: "发布日期", name: "date", widget: "datetime", format: "YYYY-MM-DD", date_format: "YYYY-MM-DD", time_format: false, required: false }
      - { label: "文章内容", name: "body", widget: "markdown", required: true }